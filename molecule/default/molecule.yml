---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint .
  ansible-lint
platforms:
  - name: instance
    image: ${MOLECULE_DISTRO:-fedora:36}
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
